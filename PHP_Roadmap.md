# 🚀 Покроковий план вивчення PHP: Від Новачка до Розробника

Цей план розроблений для послідовного вивчення. Не переходьте до наступного кроку, поки не закріпите попередній на практиці.

---

## 🌱 Рівень 1: Фундамент (Основи синтаксису)
**Мета:** Розуміти, як працює PHP, як створювати змінні та керувати потоком виконання коду.

### 1.1. Вступ та Синтаксис
*   **Теорія:**
    *   Як працює клієнт-серверна архітектура.
    *   Теги `<?php ... ?>`.
    *   Коментарі (`//`, `/* */`, `#`).
    *   Вивід даних: `echo` vs `print`.
*   **Практика:**
    *   Створити файл `index.php`, який виводить "Hello, World!".
    *   Створити HTML-сторінку, де PHP вбудований всередину HTML тегів.

### 1.2. Змінні та Типи даних
*   **Теорія:**
    *   Змінні (`$variable`).
    *   Типи: String, Integer, Float, Boolean, Null.
    *   Конкатенація рядків (`.`).
    *   Подвійні лапки `""` (інтерполяція) проти одинарних `''`.
*   **Практика:**
    *   Створити змінні `$name`, `$age` і вивести речення: "Мене звати [Ім'я], мені [Вік] років".

### 1.3. Оператори
*   **Теорія:**
    *   Арифметичні (`+`, `-`, `*`, `/`, `%`).
    *   Присвоєння (`=`, `+=`, `-=`).
    *   Порівняння (`==`, `===`, `!=`, `>`, `<`).
    *   Логічні (`&&`, `||`, `!`).
*   **Практика:**
    *   Написати калькулятор, який додає дві змінні та виводить результат.

### 1.4. Умовні конструкції
*   **Теорія:**
    *   `if`, `else`, `elseif`.
    *   Тернарний оператор (`condition ? true : false`).
    *   `switch` / `match` (сучасний PHP).
*   **Практика:**
    *   Скрипт, який перевіряє вік (`$age`): якщо < 18 — "Доступ заборонено", інакше — "Ласкаво просимо".

### 1.5. Масиви (Arrays)
*   **Теорія:**
    *   Індексовані масиви `[1, 2, 3]`.
    *   Асоціативні масиви `['key' => 'value']`.
    *   Багатовимірні масиви.
    *   Функції: `count()`, `array_push()`, `in_array()`.
*   **Практика:**
    *   Створити масив товарів з цінами. Вивести назву та ціну конкретного товару.

### 1.6. Цикли (Loops)
*   **Теорія:**
    *   `while`, `do-while`.
    *   `for`.
    *   `foreach` (найважливіший для масивів).
    *   `break`, `continue`.
*   **Практика:**
    *   Вивести список товарів з масиву у вигляді HTML списку `<ul><li>...</li></ul>`.

---

## 🛠️ Рівень 2: Структурування та Функції
**Мета:** Писати чистий код, який можна використовувати повторно.

### 2.1. Функції користувача
*   **Теорія:**
    *   Оголошення `function name() {}`.
    *   Аргументи та параметри за замовчуванням.
    *   `return` — повернення значень.
    *   Типізація аргументів (`int $a`) та повернення (`: string`).
*   **Практика:**
    *   Функція `calculateDiscount($price, $percent)`, яка повертає нову ціну.

### 2.2. Робота з рядками та вбудовані функції
*   **Теорія:**
    *   `strlen()`, `strpos()`, `str_replace()`.
    *   `explode()` (рядок в масив) та `implode()` (масив в рядок).
    *   `trim()`, `strtolower()`, `strtoupper()`.
*   **Практика:**
    *   Скрипт, який бере "брудний" рядок (з пробілами та різним регістром) і форматує його в гарне ім'я.

---

## 🌐 Рівень 3: Веб-розробка (HTTP та Форми)
**Мета:** Взаємодія з користувачем.

### 3.1. Суперглобальні масиви
*   **Теорія:**
    *   `$_GET` — дані з URL.
    *   `$_POST` — дані з форм (приховано).
    *   `$_SERVER` — інформація про сервер.
*   **Практика:**
    *   Створити посилання `page.php?name=Ivan` і вивести "Привіт, Ivan".

### 3.2. Обробка форм
*   **Теорія:**
    *   HTML форми (`<form method="POST">`).
    *   Валідація даних (чи не порожнє поле).
    *   Безпека: `htmlspecialchars()` для захисту від XSS.
*   **Практика:**
    *   Форма зворотного зв'язку (Ім'я, Email, Повідомлення). Після відправки показувати введені дані.

---

## 💾 Рівень 4: Збереження даних (Файли та Сесії)
**Мета:** Зберігати інформацію між запитами.

### 4.1. Робота з файловою системою
*   **Теорія:**
    *   `file_get_contents()`, `file_put_contents()`.
    *   `fopen()`, `fwrite()`, `fclose()`.
    *   Перевірка існування файлу `file_exists()`.
*   **Практика:**
    *   Простий логгер: записувати час кожного відвідування сторінки у файл `log.txt`.

### 4.2. Cookies та Сесії
*   **Теорія:**
    *   Cookies: зберігання на клієнті (`setcookie`).
    *   Sessions: зберігання на сервері (`session_start`, `$_SESSION`).
    *   Логіка авторизації.
*   **Практика:**
    *   Сторінка "Вхід". Якщо пароль правильний — записати в сесію і пустити на "Секретну сторінку".

---

## 🗄️ Рівень 5: Бази Даних (MySQL)
**Мета:** Професійне зберігання даних.

### 5.1. Основи SQL
*   **Теорія:**
    *   Створення таблиць.
    *   CRUD: Create (INSERT), Read (SELECT), Update (UPDATE), Delete (DELETE).
*   **Практика:**
    *   Написати SQL запити в phpMyAdmin.

### 5.2. PHP Data Objects (PDO)
*   **Теорія:**
    *   Підключення до БД.
    *   Підготовлені запити (Prepared Statements) — захист від SQL Injection.
    *   Отримання даних (`fetch`, `fetchAll`).
*   **Практика:**
    *   Створити "To-Do List" (Список справ), де завдання зберігаються в MySQL.

---

## 🏗️ Рівень 6: Об'єктно-Орієнтоване Програмування (ООП)
**Мета:** Писати складні системи.

### 6.1. Класи та Об'єкти
*   **Теорія:**
    *   `class`, `new`.
    *   Властивості та методи.
    *   `$this`.
    *   Конструктор `__construct()`.

### 6.2. Принципи ООП
*   **Теорія:**
    *   Інкапсуляція (`public`, `private`, `protected`).
    *   Наслідування (`extends`).
    *   Поліморфізм.

---

## 🎓 Фінальний проект курсу
**Створити простий Блог:**
1.  Головна сторінка зі списком статей (з БД).
2.  Сторінка перегляду однієї статті.
3.  Адмін-панель (вхід за паролем).
4.  Додавання/Редагування/Видалення статей.
